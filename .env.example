# Example environment file for docker-compose
# Copy to .env and customize for your deployment

# MongoDB
MONGO_HOST=mongo
MONGO_PORT=27017
MONGO_DB=dd_db

# Backend
NODE_ENV=production

# Frontend — no env vars required; nginx will proxy /api to backend
